import GlobalStyle from "../components/GlobalStyle";
import Frame from "../components/Frame";
import React, {ReactElement} from "react";

const Calc = (): ReactElement => {
  return (
    <>
      <GlobalStyle />
      <Frame></Frame>
    </>
  );
};

export default Calc;
